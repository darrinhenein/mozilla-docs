#Mozilla Docs

##Installation

Run `npm install` from this directory.


##Usage

1. Run `ws preview` to preview your doc and serve a live copy locally.
2. Run `ws build` to build a production ready build/ directory.